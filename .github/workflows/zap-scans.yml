name: Check run

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  zap-scan:
    runs-on: ubuntu-latest
    steps:

    - name: ZAP Scan
      uses: zaproxy/action-api-scan@v0.3.0
      with:
        target: 'http://localhost:3001'


