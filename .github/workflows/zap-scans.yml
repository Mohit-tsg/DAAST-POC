name: ZAP Scan

on:
  pull_request:
   

jobs:
  zap_scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/[email protected]

    - name: Install dependencies
      run: npm install

    - name: Run ZAP scan
      run: node zap_scan.js